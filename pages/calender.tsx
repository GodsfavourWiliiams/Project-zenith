import React from 'react';
import Layout from '../layouts';
import Views from '../views/Calender';

type Props = {}

const Calender = (props: Props) => {
    return (
        <Layout>
            <Views />
        </Layout>
  )
}

export default Calender