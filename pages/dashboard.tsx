import React from 'react';
import Views from '../views/Dashboard';
import Layout from '../layouts';

type Props = {}

const Dashboard = (props: Props) => {
  return (
    <Layout>
     <Views />
    </Layout>
  )
}

export default Dashboard;