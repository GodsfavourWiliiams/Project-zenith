import React from 'react';
import Layout from '../layouts';
import Views from '../views/Settings';

type Props = {}

const Settings = (props: Props) => {
    return (
        <Layout>
            <Views />
        </Layout>
  )
}

export default Settings